import { When } from "@wdio/cucumber-framework";
import chai from "chai";
import reporter from "../../../helper/reporter";

When(/^As an (.*) user login to nopcommerce$/, async (userType) => {
	return true;
});


When(/^Search users in customer list$/, () => {
	return true;
});




